// Calcola la somma e la media dei primi 10 numeri 

let sum = 0;
//Generatore numeri 
for( let i = 1; i <= 10; i++ ){
    // Singoli numeri da 0 a 10
    const singleNumber = i;

    // Somma dei numeri da 0 a 10
    sum += singleNumber;
}

// Media dei numeri 
const finalResult = sum / 10;


